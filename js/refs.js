/** Shared handles populated during bootstrap (map, layers, layer views). */
export const refs = {
    map: null,
    view: null,
    overlayLayers: null,
    centroidsLayerView: null,
    watershedLayerSets: null
};
